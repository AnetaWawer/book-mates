import styled from "styled-components"

export const Header = styled.div `
    position: relative;
 
`
export const Title = styled.header `
  font-size: 2em;
  line-height: 1;
  position: relative;
  margin-bottom: 75px;
  text-align: center;
`